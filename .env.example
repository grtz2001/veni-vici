# TMDB credentials — copy this file to `.env.local` and fill in ONE of the two.
# Get them at https://www.themoviedb.org/settings/api

# Recommended: the v4 "API Read Access Token"
VITE_TMDB_TOKEN=

# Or: the classic v3 API key (used if no token is set)
VITE_TMDB_API_KEY=
